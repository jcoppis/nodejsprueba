extends ../../layout.jade
block contenido
  h1 #{imagen.title}
  img(src="/public/images/#{imagen._id}.#{imagen.extension}")
  if(typeof(imagen.creator) != 'undefined')
    p Por #{imagen.creator.email}
